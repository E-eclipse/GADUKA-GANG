# Gaduka Gang Forum

Защищённая клиент-серверная информационная система на базе СУБД PostgreSQL.

## 🚀 Быстрый старт

### Требования
- Docker и Docker Compose
- Git

### Запуск через Docker Compose

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd "Gaduka Gang"
```

2. Запустите контейнеры:
```bash
docker-compose up --build
```

3. Приложение будет доступно по адресу:
- Веб-интерфейс: http://localhost:9876
- Adminer (управление БД): http://localhost:5567
- API документация: http://localhost:9876/api/v1/docs/

### Первоначальная настройка

После первого запуска создайте суперпользователя:

```bash
docker-compose exec frontend python manage.py createsuperuser
```

## 📁 Структура проекта

```
.
├── GadukaGang/              # Django приложение
│   ├── GadukaGang/         # Основное приложение
│   │   ├── models.py       # Модели БД
│   │   ├── views.py        # Веб-представления
│   │   ├── api_views.py    # API представления
│   │   ├── urls.py         # URL маршруты
│   │   └── ...
│   ├── templates/          # HTML шаблоны
│   ├── static/             # Статические файлы (CSS, JS)
│   └── manage.py
├── database/               # SQL скрипты
│   ├── forum_database.sql  # Схема БД
│   ├── views.sql           # Представления
│   ├── procedures.sql      # Хранимые процедуры
│   └── triggers.sql        # Триггеры
└── docker-compose.yaml     # Конфигурация Docker
```

## 🔧 Конфигурация

### Переменные окружения

Создайте файл `.env` в корне проекта (или используйте переменные в docker-compose.yaml):

```env
DJANGO_DEBUG=true
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1
POSTGRES_DB=forum_database
POSTGRES_USER=forum_owner
POSTGRES_PASSWORD=your_password
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
SECRET_KEY=your_secret_key_here
```

### Генерация SECRET_KEY

```bash
python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
```

## 📊 Основные функции

### База данных
- ✅ 30+ таблиц предметной области
- ✅ Нормализация до 3НФ
- ✅ Связи 1:1, 1:М, М:М
- ✅ 10 представлений (VIEW)
- ✅ 7 хранимых процедур
- ✅ 8 триггеров для аудита

### Безопасность
- ✅ RBAC (6 ролей: user, moderator, admin_level_1-3, super_admin)
- ✅ Хэширование паролей (PBKDF2)
- ✅ Шифрование/маскирование критичных полей
- ✅ Журнал аудита (SystemLog, ModeratorAction, AdminLog)
- ✅ Защита от SQL-инъекций
- ✅ Резервное копирование и восстановление

### API
- ✅ REST API для всех сущностей
- ✅ OpenAPI/Swagger документация
- ✅ Аутентификация через Token
- ✅ Импорт/экспорт CSV
- ✅ Фильтрация, поиск, пагинация

### Клиентский интерфейс
- ✅ Адаптивный дизайн (3+ брейкпоинта)
- ✅ Настройки пользователя (тема, формат даты, размер страниц)
- ✅ 12+ горячих клавиш
- ✅ Единый брендбук (палитра, шрифты, сетка)

### Аналитика
- ✅ Дашборд с визуализациями (6+ графиков)
- ✅ Фильтры по периодам
- ✅ Экспорт отчётов в CSV

## 🧪 Тестирование

### Запуск тестов безопасности
```bash
docker-compose exec frontend python manage.py test_security
```

### Запуск всех тестов
```bash
docker-compose exec frontend python manage.py test
```

## 📦 Управление данными

### Импорт/экспорт CSV
- Веб-интерфейс: http://localhost:9876/data-management/csv/
- Команды:
  ```bash
  docker-compose exec frontend python manage.py export_csv
  docker-compose exec frontend python manage.py import_csv
  ```

### Резервное копирование
- Веб-интерфейс: http://localhost:9876/data-management/backups/
- Создание бэкапа:
  ```bash
  docker-compose exec postgres pg_dump -U forum_owner forum_database > backup.sql
  ```

### Восстановление из бэкапа
```bash
docker-compose exec -T postgres psql -U forum_owner forum_database < backup.sql
```

## 🔐 Роли пользователей

- **user** - обычный пользователь (чтение, создание постов/тем)
- **moderator** - модератор (управление контентом, обработка жалоб)
- **admin_level_1** - администратор уровня 1 (управление пользователями)
- **admin_level_2** - администратор уровня 2 (управление системой)
- **admin_level_3** - администратор уровня 3 (полный доступ)
- **super_admin** - супер-администратор (все права)

## 📚 Документация

- [Описание реализации требований](РЕАЛИЗАЦИЯ_ТРЕБОВАНИЙ.md)
- [Настройка переменных окружения](GadukaGang/README_ENV.md)
- [Настройка email](GadukaGang/EMAIL_SETUP.md)
- [Настройка бэкапов](GadukaGang/BACKUP_SETUP.md)
- [Описание объектов БД](ОПИСАНИЕ_БД_ОБЪЕКТОВ.md)

## 🛠️ Разработка

### Локальная разработка (без Docker)

1. Установите зависимости:
```bash
cd GadukaGang
pip install -r requirements.txt
```

2. Настройте БД в `settings.py` или через переменные окружения

3. Примените миграции:
```bash
python manage.py migrate
```

4. Создайте суперпользователя:
```bash
python manage.py createsuperuser
```

5. Запустите сервер:
```bash
python manage.py runserver
```

## 📝 Лицензия

Учебный проект

## 👥 Авторы

Gaduka Gang Team

## 🔗 Полезные ссылки

- API документация: http://localhost:9876/api/v1/docs/
- Adminer: http://localhost:5567
- Аналитика: http://localhost:9876/analytics/ (требуется staff)

