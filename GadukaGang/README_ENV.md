# Как создать файл .env

## 📝 Быстрая инструкция

1. **Скопируйте шаблон:**
   - Файл `env_example.txt` находится в папке `GadukaGang/`
   - Переименуйте его в `.env` (уберите `_example.txt`)

2. **Заполните реальными значениями:**
   - Получите OAuth ключи (см. `ИНСТРУКЦИЯ_OAUTH.md`)
   - Замените `ваш_github_client_id` на реальный Client ID
   - Замените `ваш_github_client_secret` на реальный Secret
   - То же самое для Google OAuth

## 🔧 Через командную строку

**Windows (PowerShell):**
```powershell
cd GadukaGang
Copy-Item env_example.txt .env
```

**Windows (CMD):**
```cmd
cd GadukaGang
copy env_example.txt .env
```

**Linux/Mac:**
```bash
cd GadukaGang
cp env_example.txt .env
```

## ⚠️ Важно

- Файл `.env` уже добавлен в `.gitignore` и не будет коммититься в Git
- НЕ коммитьте файл `.env` с реальными секретами!
- После создания `.env` файла, установите `python-dotenv`:
  ```bash
  pip install python-dotenv
  ```

## 📚 Дополнительно

После создания `.env` файла:
1. Установите зависимости: `pip install -r requirements.txt`
2. Примените миграции: `python manage.py migrate`
3. Получите OAuth ключи (см. `ИНСТРУКЦИЯ_OAUTH.md`)
4. Заполните значения в `.env`
5. Перезапустите Django сервер

## ✅ Проверка

После создания `.env` файла, Django автоматически загрузит переменные из него при запуске.

