# 🔒 Документация по безопасности

## Обзор

Данный документ описывает меры безопасности, реализованные в проекте Gaduka Gang Forum для защиты данных пользователей и предотвращения несанкционированного доступа.

## 1. Аутентификация и авторизация

### 1.1 Хэширование паролей

**Технология:** Django PBKDF2 (Password-Based Key Derivation Function 2)

**Параметры:**
- Алгоритм: PBKDF2-SHA256
- Итерации: 260,000 (по умолчанию Django 4.x+)
- Соль: Автоматически генерируется для каждого пароля

**Пример хэша:**
```
pbkdf2_sha256$260000$randomsalt$hashedpassword
```

### 1.2 Ролевая модель доступа (RBAC)

**Реализованные роли:**

| Роль | Права доступа |
|------|---------------|
| `guest` | Чтение публичного контента |
| `user` | Создание тем и постов, редактирование своего контента |
| `moderator` | Модерация контента, удаление постов/тем |
| `admin_level_1` | Управление пользователями, базовое администрирование |
| `admin_level_2` | Полное администрирование, кроме критичных операций |
| `admin_level_3` | Управление бэкапами, импорт/экспорт данных |
| `super_admin` | Полный доступ ко всем функциям системы |

**Проверка прав:**
```python
from django.contrib.admin.views.decorators import staff_member_required

@staff_member_required
def sensitive_view(request):
    # Доступно только staff пользователям
    pass
```

## 2. Шифрование данных

### 2.1 Шифрование критичных полей

**Технология:** Fernet (симметричное шифрование)

**Библиотека:** `cryptography.fernet`

**Шифруемые данные:**
- Email адреса пользователей
- Номера телефонов
- Адреса проживания
- Другие персональные данные

**Пример использования:**
```python
from GadukaGang.encryption_utils import encrypt_field, decrypt_field

# Шифрование
encrypted_email = encrypt_field("user@example.com")

# Дешифрование
original_email = decrypt_field(encrypted_email)
```

### 2.2 Маскирование данных

Для отображения чувствительных данных используется маскирование:

**Email:**
```python
from GadukaGang.encryption_utils import mask_email

masked = mask_email("user@example.com")
# Результат: "u***@e***.com"
```

**Телефон:**
```python
from GadukaGang.encryption_utils import mask_phone

masked = mask_phone("+79991234567")
# Результат: "+7***67"
```

### 2.3 Хранение ключа шифрования

**⚠️ КРИТИЧНО:** Ключ шифрования НЕ должен храниться в репозитории!

**Рекомендуемые методы:**
1. Переменная окружения:
   ```bash
   export ENCRYPTION_KEY="your-fernet-key-here"
   ```

2. Файл `.env` (добавлен в `.gitignore`):
   ```
   ENCRYPTION_KEY=your-fernet-key-here
   ```

3. Секреты в production (AWS Secrets Manager, Azure Key Vault, etc.)

**Генерация ключа:**
```python
from cryptography.fernet import Fernet
key = Fernet.generate_key()
print(key.decode())
```

## 3. Защита от SQL-инъекций

### 3.1 Django ORM

Django ORM автоматически экранирует все параметры запросов:

```python
# ✅ БЕЗОПАСНО
users = User.objects.filter(username=user_input)

# ❌ НЕБЕЗОПАСНО (не используется в проекте)
cursor.execute(f"SELECT * FROM users WHERE username = '{user_input}'")
```

### 3.2 Параметризованные запросы

Для raw SQL используются параметризованные запросы:

```python
# ✅ БЕЗОПАСНО
cursor.execute(
    "SELECT * FROM users WHERE username = %s",
    [user_input]
)
```

### 3.3 Тестирование защиты

Реализован набор тестов для проверки защиты от SQL-инъекций:

```bash
python manage.py test_security
```

**Тестируемые атаки:**
1. `OR 1=1` injection
2. `UNION SELECT` injection
3. `DROP TABLE` injection
4. Comment-based injection (`--`, `#`, `/*`)
5. Stored procedure injection

**Результаты тестов:**
```
✓ Test 1: OR 1=1 injection - BLOCKED
✓ Test 2: UNION SELECT injection - BLOCKED
✓ Test 3: DROP TABLE injection - BLOCKED
✓ Test 4: Comment injection - BLOCKED
✓ Test 5: Stored procedure injection - BLOCKED
```

## 4. Защита от XSS (Cross-Site Scripting)

### 4.1 Автоматическое экранирование

Django автоматически экранирует все переменные в шаблонах:

```django
{{ user_input }}  {# Автоматически экранируется #}
```

### 4.2 CSP (Content Security Policy)

Рекомендуется настроить CSP заголовки в production:

```python
# settings.py
SECURE_CONTENT_TYPE_NOSNIFF = True
SECURE_BROWSER_XSS_FILTER = True
X_FRAME_OPTIONS = 'DENY'
```

## 5. Аудит и логирование

### 5.1 Системный журнал (SystemLog)

Все критичные действия логируются в таблицу `SystemLog`:

**Логируемые события:**
- Создание/изменение/удаление пользователей
- Изменение ролей
- Создание/изменение/удаление контента
- Операции с бэкапами
- Импорт/экспорт данных
- Попытки входа (успешные и неудачные)
- Отказы в доступе

**Сохраняемая информация:**
- Пользователь
- Тип действия
- Описание
- Временная метка
- IP адрес
- User Agent
- Данные до изменения (JSON)
- Данные после изменения (JSON)

**Пример запроса:**
```sql
SELECT 
    user_id,
    action_type,
    description,
    ip_address,
    timestamp
FROM "GadukaGang_systemlog"
WHERE action_type = 'role_changed'
ORDER BY timestamp DESC
LIMIT 10;
```

### 5.2 Middleware аудита

Автоматическое логирование через middleware:

```python
# settings.py
MIDDLEWARE = [
    ...
    'GadukaGang.middleware.audit_middleware.AuditMiddleware',
]
```

## 6. Резервное копирование

### 6.1 Создание бэкапов

```bash
# Обычный бэкап
python manage.py backup_db

# Сжатый бэкап
python manage.py backup_db --compress
```

### 6.2 Восстановление

```bash
python manage.py restore_db --file=backups/backup_20241122.sql.gz --confirm
```

### 6.3 Автоматизация

Настроен планировщик задач для ежедневного создания бэкапов в 02:00.

**Политика хранения:**
- Ежедневные бэкапы: 30 дней
- Еженедельные бэкапы: 3 месяца
- Ежемесячные бэкапы: 1 год

## 7. Защита API

### 7.1 Аутентификация API

**Метод:** Token Authentication

```python
# Получение токена
python manage.py get_api_token --username=admin

# Использование в запросах
curl -H "Authorization: Token your-token-here" http://api/endpoint/
```

### 7.2 Rate Limiting

Рекомендуется настроить ограничение частоты запросов в production:

```python
# settings.py
REST_FRAMEWORK = {
    'DEFAULT_THROTTLE_CLASSES': [
        'rest_framework.throttling.AnonRateThrottle',
        'rest_framework.throttling.UserRateThrottle'
    ],
    'DEFAULT_THROTTLE_RATES': {
        'anon': '100/day',
        'user': '1000/day'
    }
}
```

## 8. Рекомендации по безопасности

### 8.1 Production настройки

```python
# settings.py для production
DEBUG = False
ALLOWED_HOSTS = ['yourdomain.com']
SECURE_SSL_REDIRECT = True
SESSION_COOKIE_SECURE = True
CSRF_COOKIE_SECURE = True
SECURE_HSTS_SECONDS = 31536000
```

### 8.2 Регулярные обновления

- Обновлять Django и зависимости
- Мониторить CVE и security advisories
- Проводить регулярные security audits

### 8.3 Мониторинг

- Отслеживать подозрительную активность в логах
- Настроить алерты на множественные неудачные попытки входа
- Мониторить необычные паттерны доступа к API

## 9. Контрольный список безопасности

- [x] Хэширование паролей (PBKDF2)
- [x] RBAC с минимум 3 ролями
- [x] Шифрование критичных полей
- [x] Маскирование данных при отображении
- [x] Защита от SQL-инъекций (ORM + параметризованные запросы)
- [x] Защита от XSS (автоэкранирование)
- [x] Журнал аудита с IP и User Agent
- [x] Резервное копирование и восстановление
- [x] API аутентификация (Token)
- [x] Тестирование безопасности

## 10. Контакты

При обнаружении уязвимостей безопасности обращайтесь:
- Email: security@gadukagang.local
- Telegram: @security_team

---

**Последнее обновление:** 23.11.2024  
**Версия:** 1.0
