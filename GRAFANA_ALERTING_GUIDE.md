# Пошаговая инструкция: Настройка алертов в Grafana (Telegram + Email/Webhook)

## Часть 1: Создание Telegram бота

### Шаг 1: Получение токена бота
1. Откройте Telegram и найдите бота [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям: укажите имя и username для бота
4. **Скопируйте токен** (выглядит как `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

### Шаг 2: Получение Chat ID
1. Найдите вашего бота в Telegram (по username, который вы указали)
2. Отправьте боту любое сообщение (например `/start`)
3. Откройте в браузере: `https://api.telegram.org/bot<ВАШ_ТОКЕН>/getUpdates`
   - Замените `<ВАШ_ТОКЕН>` на токен из шага 1
4. Найдите в JSON ответе поле `"chat":{"id":123456789}` — это ваш Chat ID
5. **Скопируйте Chat ID** (число, может быть отрицательным для групп)

---

## Часть 2: Настройка Contact Points в Grafana

### Шаг 3: Открытие раздела Alerting
1. Войдите в Grafana: `http://localhost:3000` (admin/admin)
2. В левом меню нажмите на иконку **колокольчика** (Alerting) или перейдите в **Alerting → Contact points**

### Шаг 4: Создание Telegram Contact Point
1. Нажмите **"New contact point"** или **"Add contact point"**
2. Заполните форму:
   - **Name**: `Telegram Bot` (или любое удобное имя)
   - **Integration**: выберите **"Telegram"** из списка
3. В поле **"BOT API Token"** вставьте токен из шага 1
4. В поле **"Chat ID"** вставьте Chat ID из шага 2
5. В поле **"Message"** можно оставить шаблон по умолчанию или настроить:
   ```
   🔔 Alert: {{ .CommonLabels.alertname }}
   Status: {{ .Status }}
   {{ range .Alerts }}
   {{ .Annotations.description }}
   {{ end }}
   ```
6. Нажмите **"Test"** — должно прийти тестовое сообщение в Telegram
7. Нажмите **"Save contact point"**

### Шаг 5: Создание второго Contact Point (Email)
1. Снова нажмите **"New contact point"**
2. Заполните:
   - **Name**: `Email Notifications`
   - **Integration**: выберите **"Email"**
3. В поле **"Addresses"** укажите email адреса через запятую (например: `admin@example.com, dev@example.com`)
4. **Важно**: Email требует настройки SMTP в Grafana (см. ниже)
5. Нажмите **"Save contact point"**

#### Настройка SMTP для Email (через переменные окружения в docker-compose):

**Способ 1: Через переменные окружения (рекомендуется)**

1. Откройте файл `docker-compose.yaml`
2. Найдите секцию `grafana` → `environment`
3. Добавьте или измените переменные (пример для Gmail):
   ```yaml
   - GF_SMTP_ENABLED=true
   - GF_SMTP_HOST=smtp.gmail.com
   - GF_SMTP_PORT=587
   - GF_SMTP_USER=ваш_email@gmail.com
   - GF_SMTP_PASSWORD=ваш_пароль_приложения  # Для Gmail нужен "Пароль приложения"
   - GF_SMTP_FROM_ADDRESS=ваш_email@gmail.com
   - GF_SMTP_SKIP_VERIFY=false
   ```
4. Перезапустите Grafana: `docker compose restart grafana`

**Способ 2: Через .env файл (безопаснее)**

1. Создайте файл `.env` в корне проекта (если его нет)
2. Добавьте туда:
   ```
   GRAFANA_SMTP_ENABLED=true
   GRAFANA_SMTP_HOST=smtp.gmail.com
   GRAFANA_SMTP_PORT=587
   GRAFANA_SMTP_USER=ваш_email@gmail.com
   GRAFANA_SMTP_PASSWORD=ваш_пароль_приложения
   GRAFANA_SMTP_FROM_ADDRESS=ваш_email@gmail.com
   GRAFANA_SMTP_SKIP_VERIFY=false
   ```
3. Перезапустите: `docker compose restart grafana`

**Важно для Gmail:**
- Используйте **"Пароль приложения"**, а не обычный пароль
- Получить можно: Google Account → Security → 2-Step Verification → App passwords
- Создайте пароль для "Mail" и "Other (Custom name)" → "Grafana"

**Способ 3: Через UI Grafana (если переменные не работают)**

1. Перейдите в **Administration → Settings → SMTP**
2. Заполните настройки:
   - **Enabled**: включите чекбокс
   - **Host**: `smtp.gmail.com` (или ваш SMTP)
   - **Port**: `587`
   - **User**: ваш email
   - **Password**: пароль приложения (для Gmail)
   - **From address**: адрес отправителя
3. Нажмите **"Save"**
4. Нажмите **"Send test email"** для проверки

### Альтернатива: Webhook вместо Email (рекомендуется для тестирования)
Если Email не работает или не хотите настраивать SMTP, используйте Webhook:

1. **Name**: `Webhook Notifications`
2. **Integration**: выберите **"Webhook"**
3. **URL**: укажите URL вашего webhook-сервиса

**Для тестирования используйте [webhook.site](https://webhook.site):**
   - Откройте https://webhook.site
   - Скопируйте уникальный URL (например: `https://webhook.site/12345678-1234-1234-1234-123456789abc`)
   - Вставьте в поле **URL** в Grafana
   - Все уведомления будут видны на webhook.site в реальном времени

**Для продакшена можно использовать:**
   - Slack: `https://hooks.slack.com/services/YOUR/WEBHOOK/URL`
   - Discord: `https://discord.com/api/webhooks/YOUR/WEBHOOK/URL`
   - Microsoft Teams: `https://outlook.office.com/webhook/YOUR/WEBHOOK/URL`

---

## Часть 3: Создание Alert Rules

### Шаг 6: Создание Alert Rule
1. Перейдите в **Alerting → Alert rules**
2. Нажмите **"New alert rule"**

### Шаг 7: Настройка Query (метрика для мониторинга)
1. В разделе **"Query"** выберите datasource **"Prometheus"**
2. В поле **"Query"** введите один из запросов:

   **Пример 1: Высокая задержка ответов**
   ```
   histogram_quantile(0.95, rate(django_http_requests_latency_seconds_bucket[5m])) > 1
   ```
   Это сработает, если 95-й перцентиль задержки превышает 1 секунду.

   **Пример 2: Много 404 ошибок**
   ```
   rate(django_http_requests_total{status="404"}[5m]) > 0.1
   ```
   Сработает, если больше 0.1 запросов в секунду возвращают 404.

   **Пример 3: Падение количества активных пользователей**
   ```
   gaduka_active_users_total < 5
   ```
   Сработает, если активных пользователей меньше 5.

   **Пример 4: Высокая нагрузка на сервер**
   ```
   django_http_requests_in_progress > 50
   ```
   Сработает, если одновременно обрабатывается больше 50 запросов.

3. В поле **"Legend"** укажите: `{{status}}` или `High Latency` (для читаемости)

### Шаг 8: Настройка условий (Conditions)
1. В разделе **"Conditions"**:
   - **WHEN**: выберите `last()`
   - **OF**: выберите `A` (ваш query)
   - **IS ABOVE**: укажите порог (например, `0.1` для rate или `1` для latency)
2. Это означает: "когда последнее значение превышает порог"

### Шаг 9: Настройка оценки (Evaluation)
1. **Evaluate every**: `30s` (как часто проверять)
2. **For**: `2m` (сколько времени условие должно выполняться перед срабатыванием)
   - Это создаст состояние **Pending** на 2 минуты, затем перейдёт в **Firing**

### Шаг 10: Настройка уведомлений (Notifications)
1. В разделе **"Notifications"**:
   - **Contact point**: выберите `Telegram Bot` (созданный в шаге 4)
   - **Additional contact points**: добавьте `Email Notifications` (или Webhook)
2. В поле **"Summary"** укажите краткое описание: `High latency detected on GadukaGang`
3. В поле **"Description"** укажите детали:
   ```
   Alert: {{ $labels.alertname }}
   Current value: {{ $value }}
   Threshold: 1 second
   ```

### Шаг 11: Сохранение и тестирование
1. Вверху укажите **"Rule name"**: `High Latency Alert` (или другое имя)
2. Нажмите **"Save rule"**
3. Подождите 2-3 минуты — правило начнёт оцениваться

---

## Часть 4: Демонстрация состояний алерта

### Состояние Normal (норма)
- Алерт не срабатывает, условие не выполнено
- В разделе **Alerting → Alert rules** статус будет **"Normal"** (зелёный)
- Уведомления не отправляются

### Состояние Pending (ожидание)
1. Условие выполнено, но прошло меньше времени, чем указано в **"For"** (например, меньше 2 минут)
2. В Grafana статус будет **"Pending"** (жёлтый)
3. Уведомления ещё не отправляются

### Состояние Firing (срабатывание)
1. Условие выполнено и прошло время из **"For"** (например, 2 минуты)
2. Статус меняется на **"Firing"** (красный)
3. **Отправляется уведомление в Telegram** с текстом:
   ```
   🔔 Alert: High Latency Alert
   Status: firing
   Current value: 1.5
   Threshold: 1 second
   ```
4. **Отправляется уведомление на Email/Webhook** (если настроено)

### Состояние Resolved (решено)
1. Условие перестало выполняться (метрика вернулась в норму)
2. Статус меняется на **"Resolved"** (зелёный)
3. **Отправляется уведомление в Telegram**:
   ```
   ✅ Alert: High Latency Alert
   Status: resolved
   Alert has been resolved.
   ```
4. **Отправляется уведомление на Email/Webhook**

---

## Часть 5: Как протестировать алерт

### Тест 1: Создать условие для срабатывания
1. Создайте алерт на метрику `gaduka_active_users_total < 100` (если у вас меньше 100 пользователей, он сразу сработает)
2. Или создайте алерт на `rate(django_http_requests_total{status="404"}[5m]) > 0`
3. Затем откройте несуществующую страницу несколько раз: `http://localhost:9876/nonexistent-page`
4. Через 2-3 минуты должен прийти Firing в Telegram

### Тест 2: Проверить Resolved
1. После получения Firing, исправьте условие (например, перестаньте генерировать 404)
2. Подождите ещё 2-3 минуты
3. Должно прийти Resolved в Telegram

---

## Часть 6: Просмотр истории алертов

1. Перейдите в **Alerting → Alert rules**
2. Нажмите на название вашего правила
3. Внизу будет вкладка **"State history"** — там видна вся история:
   - Когда был Normal
   - Когда перешёл в Pending
   - Когда стал Firing
   - Когда Resolved

---

## Полезные PromQL запросы для алертов

```promql
# Высокая задержка (P95 > 1 сек)
histogram_quantile(0.95, rate(django_http_requests_latency_seconds_bucket[5m])) > 1

# Много 404 ошибок (> 5 в минуту)
rate(django_http_requests_total{status="404"}[5m]) * 60 > 5

# Много 500 ошибок
rate(django_http_requests_total{status="500"}[5m]) > 0.01

# Высокая нагрузка (много одновременных запросов)
django_http_requests_in_progress > 50

# Падение количества пользователей
gaduka_active_users_total < 10

# Нет новых постов за час
rate(gaduka_posts_total[1h]) == 0
```

---

## Troubleshooting

### Telegram бот не отправляет сообщения
- Проверьте токен и Chat ID
- Убедитесь, что бот запущен (отправьте ему `/start`)
- Проверьте, что в Grafana Contact Point указаны правильные данные

### Email не работает
- **Проверьте переменные окружения**: убедитесь, что `GF_SMTP_ENABLED=true` и все параметры заполнены
- **Для Gmail нужен "Пароль приложения"** (не обычный пароль):
  - Google Account → Security → 2-Step Verification → App passwords
  - Создайте пароль для "Mail" и "Other (Custom name)" → "Grafana"
- **Проверьте логи Grafana**: `docker logs gadukagang-grafana | grep -i smtp`
- **Проверьте настройки в UI**: Administration → Settings → SMTP → Send test email
- **Альтернатива**: используйте Webhook (webhook.site для тестирования) — проще и быстрее

### Алерт не срабатывает
- Убедитесь, что метрика существует: проверьте в Prometheus `http://localhost:9090`
- Проверьте условие: возможно, порог слишком высокий
- Уменьшите **"For"** до `1m` для быстрого тестирования

