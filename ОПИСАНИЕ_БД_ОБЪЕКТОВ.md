# 📚 Описание функций, процедур, триггеров и представлений

## 🔧 Хранимые процедуры (7 штук)

### 1. `calculate_user_statistics(user_id)` 
**Что делает:** Рассчитывает полную статистику пользователя

**Возвращает:**
- `total_posts` - Общее количество постов пользователя
- `total_topics` - Общее количество созданных тем
- `total_likes` - Сумма всех лайков на постах
- `total_dislikes` - Сумма всех дизлайков
- `achievements_count` - Количество полученных достижений
- `rank_name` - Название текущего ранга
- `rank_progress` - Прогресс до следующего ранга (%)

**Пример использования:**
```bash
python manage.py call_procedure calculate_user_statistics --user-id=1
```

**Зачем нужна:** Быстро получить всю статистику пользователя одним запросом вместо множества отдельных запросов к разным таблицам.

---

### 2. `batch_update_topic_ratings()`
**Что делает:** Пакетно пересчитывает средний рейтинг и количество оценок для ВСЕХ тем форума

**Возвращает:**
- `updated_count` - Количество обновлённых тем
- `execution_time` - Время выполнения операции

**Пример использования:**
```bash
python manage.py call_procedure batch_update_topic_ratings
```

**Зачем нужна:** 
- Исправление несоответствий в рейтингах
- Периодическое обновление статистики
- Восстановление данных после сбоя

---

### 3. `archive_old_posts(days_threshold)`
**Что делает:** Помечает старые посты как удалённые (мягкое удаление)

**Параметры:**
- `days_threshold` - Порог в днях (по умолчанию 365)

**Возвращает:**
- `archived_count` - Количество архивированных постов
- `oldest_post_date` - Дата самого старого архивированного поста

**Пример использования:**
```bash
# Архивировать посты старше 2 лет
python manage.py call_procedure archive_old_posts --days=730
```

**Зачем нужна:**
- Очистка старых неактуальных постов
- Оптимизация производительности БД
- Соблюдение политики хранения данных

---

### 4. `generate_analytics_report(date_from, date_to)`
**Что делает:** Генерирует аналитический отчёт с метриками и их изменениями

**Параметры:**
- `date_from` - Начало периода (по умолчанию: 30 дней назад)
- `date_to` - Конец периода (по умолчанию: сегодня)

**Возвращает для каждой метрики:**
- `metric_name` - Название метрики
- `metric_value` - Значение за период
- `metric_change_percent` - Изменение по сравнению с предыдущим периодом (%)

**Метрики:**
1. Новые пользователи
2. Новые темы
3. Новые посты
4. Активные пользователи

**Пример использования:**
```bash
# Отчёт за последний месяц
python manage.py call_procedure generate_analytics_report

# Отчёт за конкретный период
python manage.py call_procedure generate_analytics_report --date-from=2024-01-01 --date-to=2024-12-31
```

**Зачем нужна:**
- Анализ роста форума
- Отслеживание активности
- Принятие управленческих решений

---

### 5. `award_achievements_batch()`
**Что делает:** Массово выдаёт достижения пользователям, которые выполнили критерии

**Проверяемые достижения:**
- "Первое сообщение" - 1+ пост
- "Активный участник" - 50+ постов

**Возвращает:**
- `user_id` - ID пользователя
- `achievement_id` - ID достижения
- `achievement_name` - Название достижения

**Пример использования:**
```bash
python manage.py call_procedure award_achievements_batch
```

**Зачем нужна:**
- Автоматическая геймификация
- Периодическая проверка критериев
- Мотивация пользователей

---

### 6. `process_complaint_transaction(complaint_id, moderator_id, new_status, action_description)`
**Что делает:** Обрабатывает жалобу с полным логированием в рамках ТРАНЗАКЦИИ

**Параметры:**
- `complaint_id` - ID жалобы
- `moderator_id` - ID модератора
- `new_status` - Новый статус ('resolved', 'dismissed', 'in_review')
- `action_description` - Описание действия

**Что происходит внутри:**
1. Проверка существования жалобы
2. Обновление статуса жалобы
3. Логирование действия модератора в `moderator_actions`
4. Логирование в системный лог `system_logs`
5. При ошибке - откат ВСЕХ изменений

**Возвращает:**
- `TRUE` - успешно
- `FALSE` - ошибка (с откатом)

**Пример использования (Python):**
```python
from GadukaGang.db_procedures import DatabaseProcedures

result = DatabaseProcedures.process_complaint_transaction(
    complaint_id=1,
    moderator_id=1,
    new_status='resolved',
    action_description='Жалоба проверена, нарушений не найдено'
)
```

**Зачем нужна:**
- Атомарность операций модерации
- Полный аудит действий
- Защита от частичных обновлений при ошибках

---

### 7. `update_user_ranks()`
**Что делает:** Автоматически обновляет ранги пользователей на основе накопленных очков

**Возвращает:**
- `user_id` - ID пользователя
- `old_rank` - Старый ранг
- `new_rank` - Новый ранг
- `current_points` - Текущие очки

**Пример использования:**
```bash
python manage.py call_procedure update_user_ranks
```

**Зачем нужна:**
- Автоматическое повышение рангов
- Периодическая синхронизация
- Геймификация системы

---

## ⚡ Триггеры (8 штук)

### 1. `trigger_audit_user_changes`
**Таблица:** `GadukaGang_user`  
**Событие:** UPDATE, DELETE

**Что делает:**
- Логирует изменение роли пользователя
- Логирует изменение статуса активности (is_active)
- Логирует удаление пользователя

**Куда пишет:** `GadukaGang_systemlog`

**Пример лога:**
```
action_type: 'role_changed'
description: 'Роль изменена с "user" на "moderator"'
```

**Зачем нужен:** Аудит критичных изменений пользователей для безопасности

---

### 2. `trigger_audit_post_changes`
**Таблица:** `GadukaGang_post`  
**Событие:** UPDATE

**Что делает:**
- Логирует редактирование контента поста
- Логирует удаление поста (is_deleted = TRUE)

**Куда пишет:** `GadukaGang_systemlog`

**Пример лога:**
```
action_type: 'post_edited'
description: 'Пост #123 отредактирован (редакция #2)'
```

**Зачем нужен:** История изменений постов для модерации

---

### 3. `trigger_update_last_activity_on_post`
**Таблица:** `GadukaGang_post`  
**Событие:** INSERT, UPDATE

**Что делает:**
- Автоматически обновляет `last_activity` в `UserProfile` при создании/редактировании поста

**Пример:**
```
Пользователь создал пост → last_activity = ТЕКУЩЕЕ ВРЕМЯ
```

**Зачем нужен:** Автоматическое отслеживание активности пользователей

---

### 4. `trigger_update_post_count`
**Таблица:** `GadukaGang_post`  
**Событие:** INSERT, DELETE, UPDATE

**Что делает:**
- При создании поста: `post_count + 1`
- При удалении поста: `post_count - 1`
- При пометке как удалённого: `post_count - 1`
- При восстановлении: `post_count + 1`

**Куда обновляет:** `GadukaGang_userprofile.post_count`

**Зачем нужен:** Автоматический подсчёт постов без ручных обновлений

---

### 5. `trigger_update_topic_last_post`
**Таблица:** `GadukaGang_post`  
**Событие:** INSERT

**Что делает:**
- Обновляет `last_post_date` в теме при добавлении нового поста

**Пример:**
```
Новый пост в теме → last_post_date = дата создания поста
```

**Зачем нужен:** Сортировка тем по последней активности

---

### 6. `trigger_audit_moderator_actions`
**Таблица:** `GadukaGang_moderatoraction`  
**Событие:** INSERT

**Что делает:**
- Дополнительно логирует КРИТИЧНЫЕ действия модераторов:
  - `delete_post` - удаление поста
  - `delete_topic` - удаление темы
  - `block_user` - блокировка пользователя

**Куда пишет:** `GadukaGang_systemlog`

**Зачем нужен:** Усиленный аудит критичных модераторских действий

---

### 7. `trigger_validate_subscription`
**Таблица:** `GadukaGang_usersubscription`  
**Событие:** INSERT, UPDATE (BEFORE)

**Что делает:**
- Проверяет, что пользователь не подписывается сам на себя
- При попытке: выбрасывает ошибку

**Пример:**
```sql
INSERT INTO subscriptions (subscriber_id=1, subscribed_to_id=1)
→ ОШИБКА: "Пользователь не может подписаться сам на себя"
```

**Зачем нужен:** Валидация бизнес-правил на уровне БД

---

### 8. `trigger_award_points_for_achievement`
**Таблица:** `GadukaGang_userachievement`  
**Событие:** INSERT

**Что делает:**
- Начисляет 10 очков при получении достижения
- Логирует выдачу достижения

**Куда обновляет:** `GadukaGang_userrankprogress.current_points`  
**Куда пишет:** `GadukaGang_systemlog`

**Зачем нужен:** Автоматическая система прогрессии рангов

---

## 👁️ Представления (VIEW) (10 штук)

### 1. `v_user_statistics`
**Что показывает:** Полная статистика каждого пользователя

**Поля:**
- Базовая информация (username, email, role)
- Количество постов
- Количество созданных тем
- Количество достижений
- Ранг и прогресс
- Лайки/дизлайки

**SQL:**
```sql
SELECT * FROM v_user_statistics WHERE username = 'eclipse';
```

**Зачем нужно:** Быстрый доступ к статистике без JOIN-ов

---

### 2. `v_topic_statistics`
**Что показывает:** Статистика по каждой теме

**Поля:**
- Название темы и раздел
- Количество постов
- Количество уникальных авторов
- Количество тегов
- Просмотры и рейтинг
- Возраст темы в днях

**SQL:**
```sql
SELECT * FROM v_topic_statistics ORDER BY posts_count DESC LIMIT 10;
```

**Зачем нужно:** Анализ популярности тем

---

### 3. `v_active_users_24h`
**Что показывает:** Пользователи, активные за последние 24 часа

**Поля:**
- Информация о пользователе
- Время последней активности
- Количество постов за 24ч
- Количество тем за 24ч

**SQL:**
```sql
SELECT * FROM v_active_users_24h;
```

**Зачем нужно:** Мониторинг текущей активности форума

---

### 4. `v_section_statistics`
**Что показывает:** Статистика по каждому разделу форума

**Поля:**
- Название раздела
- Количество тем и постов
- Количество уникальных авторов
- Средний рейтинг тем
- Дата последней активности

**SQL:**
```sql
SELECT * FROM v_section_statistics ORDER BY topics_count DESC;
```

**Зачем нужно:** Анализ популярности разделов

---

### 5. `v_top_contributors`
**Что показывает:** Рейтинг самых активных авторов

**Поля:**
- Информация о пользователе
- Количество постов и тем
- Лайки/дизлайки
- Ранг по постам
- Достижения

**SQL:**
```sql
SELECT * FROM v_top_contributors LIMIT 10;
```

**Зачем нужно:** Геймификация, мотивация пользователей

---

### 6. `v_daily_activity`
**Что показывает:** Активность по дням за последние 30 дней

**Поля:**
- Дата
- Новые пользователи
- Новые темы
- Новые посты
- Активные пользователи

**SQL:**
```sql
SELECT * FROM v_daily_activity ORDER BY activity_date DESC;
```

**Зачем нужно:** Графики активности, тренды

---

### 7. `v_popular_tags`
**Что показывает:** Самые популярные теги

**Поля:**
- Название тега
- Количество тем с этим тегом
- Количество уникальных авторов
- Общие просмотры
- Средний рейтинг

**SQL:**
```sql
SELECT * FROM v_popular_tags LIMIT 20;
```

**Зачем нужно:** Облако тегов, навигация по темам

---

### 8. `v_moderation_statistics`
**Что показывает:** Статистика работы модераторов

**Поля:**
- Информация о модераторе
- Количество действий
- Удалённые посты/темы
- Заблокированные пользователи
- Обработанные жалобы

**SQL:**
```sql
SELECT * FROM v_moderation_statistics ORDER BY total_actions DESC;
```

**Зачем нужно:** Контроль работы модераторов

---

### 9. `v_pending_complaints`
**Что показывает:** Жалобы, ожидающие рассмотрения

**Поля:**
- Информация о жалобе
- Кто пожаловался
- Назначенный модератор
- Время ожидания (в часах)

**SQL:**
```sql
SELECT * FROM v_pending_complaints ORDER BY created_date ASC;
```

**Зачем нужно:** Очередь работы для модераторов

---

### 10. `v_system_activity`
**Что показывает:** Системная активность (логи)

**Поля:**
- Тип действия
- Пользователь
- Описание
- Затронутый ресурс
- Дата и час

**SQL:**
```sql
SELECT * FROM v_system_activity ORDER BY timestamp DESC LIMIT 100;
```

**Зачем нужно:** Аудит, безопасность, отладка

---

## 📊 Итоговая таблица

| Тип объекта | Количество | Основная цель |
|-------------|------------|---------------|
| Процедуры | 7 | Сложные операции, аналитика, пакетные обновления |
| Триггеры | 8 | Автоматизация, аудит, валидация |
| VIEW | 10 | Быстрый доступ к аналитике, упрощение запросов |

**Общая польза:**
- ✅ Автоматизация рутинных операций
- ✅ Полный аудит всех действий
- ✅ Целостность данных
- ✅ Производительность (меньше запросов)
- ✅ Безопасность (валидация на уровне БД)
