# Корректировки и рекомендации к ТЗ веб-форума "Gaduka Gang"

## 🔍 Анализ текущего состояния

### ✅ Что уже реализовано
- Базовая структура Django проекта
- Docker Compose конфигурация
- PostgreSQL база данных с триггерами и функциями
- Основные модели данных (большая часть)
- Базовая аутентификация (регистрация, вход, выход)
- Профили пользователей (базовая версия)
- Главная страница (базовая версия)

### ❌ Что отсутствует
- Недостающие модели (лайки, подписки, уведомления и др.)
- GitHub OAuth интеграция
- Полноценный CRUD для тем и сообщений
- Система поиска
- Админ-панель с разными уровнями доступа
- Система чатов (UI)
- Модерация (UI и логика)

---

## 📋 Корректировки к ТЗ

### 1. Структура базы данных

#### 1.1. Роли пользователей
**ТЗ:** Упоминается таблица `roles` и `user_roles` (M:M связь)  
**Текущая реализация:** Роль хранится в поле `role` модели `User`  
**Рекомендация:** 
- ✅ Оставить текущую реализацию (проще и достаточно для проекта)
- Если в будущем понадобится множественность ролей, можно мигрировать на M:M
- Текущая реализация соответствует требованиям ТЗ по функциональности

#### 1.2. Таблица search_index
**ТЗ:** Упоминается отдельная таблица `search_index` для полнотекстового поиска  
**Рекомендация:**
- Использовать встроенный PostgreSQL Full-Text Search
- Создать GIN индексы на полях `title` и `content` в таблицах `topics` и `posts`
- Отдельную таблицу можно использовать для кэширования популярных запросов (опционально)

#### 1.3. Логи модераторов и администраторов
**ТЗ:** Упоминаются отдельные таблицы `moderator_actions` и `admin_logs`  
**Текущая реализация:** Есть общая таблица `system_logs`  
**Рекомендация:**
- ✅ Создать отдельные модели `ModeratorAction` и `AdminLog` для лучшей организации
- Это позволит легче фильтровать логи по типу действия и уровню доступа
- Общую таблицу `SystemLog` можно оставить для системных событий

### 2. Функциональные требования

#### 2.1. Система уведомлений
**ТЗ:** Упоминается в функционале, но не описана детально  
**Рекомендация:**
- Добавить модель `Notification` для хранения уведомлений
- Типы уведомлений:
  - Новое сообщение в подписанной теме
  - Ответ на ваше сообщение
  - Получение лайка/дизлайка
  - Получение достижения
  - Жалоба обработана
  - Приглашение в групповой чат
- Реализовать систему "прочитано/не прочитано"

#### 2.2. Оценки тем
**ТЗ:** "Выставлять оценку теме"  
**Рекомендация:**
- Создать модель `TopicRating` с полями:
  - `topic` (ForeignKey)
  - `user` (ForeignKey)
  - `rating` (IntegerField, 1-5)
  - `created_date` (DateTimeField)
- Ограничение: один пользователь может оценить тему только один раз
- Вычислять средний рейтинг темы для отображения

#### 2.3. Лайки/дизлайки сообщений
**ТЗ:** "Лайкать/дизлайкать чужие сообщения"  
**Рекомендация:**
- Создать модель `PostLike` с полями:
  - `post` (ForeignKey)
  - `user` (ForeignKey)
  - `like_type` (CharField: 'like' или 'dislike')
  - `created_date` (DateTimeField)
- Ограничение: один пользователь может поставить только один лайк или дизлайк на сообщение
- Возможность изменить лайк на дизлайк и наоборот

#### 2.4. Подписки
**ТЗ:** "Подписываться на других пользователей" и "Отслеживать авторов"  
**Рекомендация:**
- Создать модель `UserSubscription` для подписок на пользователей
- Создать модель `TopicSubscription` для подписок на темы
- Реализовать уведомления при новой активности у подписанных объектов

### 3. Технические аспекты

#### 3.1. GitHub OAuth
**ТЗ:** "Регистрироваться через GitHub"  
**Рекомендация:**
- Использовать библиотеку `django-allauth` (самая популярная и функциональная)
- Создать модель `GitHubAuth` для хранения связи с GitHub аккаунтом
- Поля модели:
  - `user` (OneToOneField)
  - `github_id` (CharField, unique)
  - `github_username` (CharField)
  - `access_token` (CharField, зашифрованный)
  - `created_date` (DateTimeField)

#### 3.2. Поиск
**ТЗ:** Детально описан поиск по различным критериям  
**Рекомендация:**
- Использовать PostgreSQL Full-Text Search для базового поиска
- Для сложных запросов можно использовать `django-postgres-search` или `django-haystack`
- Реализовать поиск по:
  - Ключевым словам (заголовки и содержимое)
  - Тегам
  - Автору
  - Дате создания
  - Разделу
  - Рейтингу/популярности

#### 3.3. Real-time чаты
**ТЗ:** Упоминаются чаты, но не указано, нужен ли real-time  
**Рекомендация:**
- Для MVP: обычные HTTP запросы (polling или long polling)
- Для production: WebSocket через Django Channels (если нужен real-time)
- Это значительно усложнит архитектуру, поэтому можно отложить на второй этап

### 4. Админ-панель

#### 4.1. Уровни доступа
**ТЗ:** Детально описаны 4 уровня администраторов  
**Рекомендация:**
- Создать отдельные views/URLs для каждого уровня
- Использовать декораторы и mixins для проверки прав доступа
- Структура:
  ```
  /admin/level1/  - для модераторов
  /admin/level2/  - для админов уровня 2
  /admin/level3/  - для админов уровня 3
  /admin/super/   - для главного админа
  ```

#### 4.2. Статистика
**ТЗ:** Различная статистика для разных уровней  
**Рекомендация:**
- Использовать представления (views) в PostgreSQL для оптимизации запросов
- Кэшировать часто запрашиваемую статистику (Redis или Django cache)
- Создать отдельные модули для каждого типа статистики

### 5. Безопасность

#### 5.1. Дополнительные меры
**ТЗ:** Базовые требования к безопасности  
**Рекомендация:**
- ✅ Django уже предоставляет CSRF защиту
- ✅ ORM защищает от SQL injection
- Добавить:
  - Rate limiting для API endpoints (django-ratelimit)
  - Валидацию всех входных данных
  - Sanitization HTML контента (bleach)
  - Логирование подозрительной активности

#### 5.2. Хранение паролей
**ТЗ:** Пароли должны быть хэшированы  
**Рекомендация:**
- ✅ Django использует PBKDF2 по умолчанию (безопасно)
- Можно настроить более сильный алгоритм (Argon2) в settings.py

### 6. Производительность

#### 6.1. Оптимизация запросов
**Рекомендация:**
- Использовать `select_related()` и `prefetch_related()` везде, где нужно
- Создать индексы в БД для часто используемых полей
- Использовать пагинацию для всех списков
- Кэшировать популярные запросы

#### 6.2. Масштабируемость
**Рекомендация:**
- Использовать Redis для кэширования
- Настроить Celery для фоновых задач (отправка email, генерация отчетов)
- Использовать CDN для статических файлов в production

---

## 🎨 UI/UX рекомендации

### 1. Дизайн
- Использовать современный CSS фреймворк (Bootstrap 5 или Tailwind CSS)
- Адаптивный дизайн (mobile-first подход)
- Темная/светлая тема (опционально, но желательно)

### 2. Интерактивность
- AJAX для всех действий (лайки, оценки, подписки) - без перезагрузки страницы
- Real-time уведомления (можно через polling для MVP)
- Автозаполнение при поиске
- Предпросмотр Markdown при написании сообщений

### 3. Доступность
- Семантический HTML
- ARIA атрибуты где необходимо
- Поддержка клавиатурной навигации
- Контрастность цветов (WCAG AA минимум)

---

## 📦 Рекомендуемые библиотеки

### Обязательные
```txt
Django==5.2.6
psycopg2-binary==2.9.10
django-allauth==0.57.0  # GitHub OAuth
Pillow==10.4.0  # Работа с изображениями
```

### Рекомендуемые
```txt
django-markdownx==4.0.1  # Markdown редактор
django-crispy-forms==2.3  # Красивые формы
crispy-bootstrap5==2024.2  # Bootstrap для форм
django-ratelimit==4.1.0  # Rate limiting
bleach==6.1.0  # Sanitization HTML
redis==5.2.0  # Кэширование
celery==5.4.0  # Фоновые задачи (опционально)
```

### Опциональные (для расширенного функционала)
```txt
channels==4.0.0  # WebSocket для real-time
channels-redis==4.2.0  # Redis для channels
django-haystack==3.2.1  # Расширенный поиск
```

---

## 🔄 Миграции и обновления

### 1. Добавление недостающих моделей
Создать миграции для:
- PostLike
- TopicRating
- UserSubscription
- TopicSubscription
- ModeratorAction
- AdminLog
- Notification
- GitHubAuth (опционально, если используем django-allauth)

### 2. Обновление существующих моделей
- Добавить поле `rating` в модель `Topic` (для хранения среднего рейтинга)
- Добавить поле `like_count` и `dislike_count` в модель `Post` (для оптимизации)
- Добавить поле `github_id` в модель `User` (если не используем отдельную модель)

---

## 📊 Приоритизация задач

### Критично (для MVP)
1. ✅ Доработка моделей (добавить недостающие)
2. ✅ Базовый CRUD для тем и сообщений
3. ✅ Система аутентификации (включая GitHub OAuth)
4. ✅ Профили пользователей
5. ✅ Базовый поиск

### Важно (для полноценного функционала)
1. Система оценок (лайки, рейтинги)
2. Подписки
3. Система достижений (автоматическая выдача)
4. Базовая модерация
5. Админ-панель уровня 1

### Желательно (для улучшения UX)
1. Real-time чаты
2. Расширенная аналитика
3. Экспорт данных
4. Темная тема
5. Мобильное приложение (дальнейшее развитие)

---

## ✅ Чеклист соответствия ТЗ

### База данных
- [x] Все 23 таблицы из ТЗ (большинство реализовано)
- [ ] Добавить недостающие таблицы (лайки, подписки, уведомления)
- [x] Триггеры для автоматического обновления счетчиков
- [x] Функции для проверки прав и расчетов
- [x] Представления (views) для статистики

### Функционал пользователя
- [x] Регистрация и авторизация
- [ ] Регистрация через GitHub (требует реализации)
- [x] Создание и просмотр тем
- [ ] Написание сообщений (требует UI)
- [ ] Редактирование/удаление своих сообщений
- [x] Просмотр профилей
- [ ] Получение достижений (требует автоматизации)
- [ ] Просмотр статистики
- [ ] Подписки на пользователей (требует реализации)
- [ ] Оценка тем (требует реализации)
- [ ] Лайки/дизлайки сообщений (требует реализации)

### Функционал модератора
- [ ] Удаление и редактирование чужих сообщений/тем
- [ ] Блокировка/разблокировка пользователей
- [ ] Закрепление тем
- [ ] Отправка сообщений от лица "Главного проекта"
- [ ] Выдача званий и рангов
- [ ] Просмотр жалоб

### Функционал администратора
- [ ] Назначение/удаление модераторов
- [ ] Просмотр статистики по всем пользователям
- [ ] Управление настройками форума
- [ ] Создание и удаление разделов
- [ ] Просмотр логов действий

### Админ-панель
- [ ] Уровень 1 (модератор) - все функции
- [ ] Уровень 2 - расширенные функции
- [ ] Уровень 3 - системные функции
- [ ] Главный администратор - полный доступ

---

## 🎯 Итоговые рекомендации

1. **Начать с MVP** - реализовать базовый функционал для работы форума
2. **Постепенно добавлять функции** - не пытаться реализовать всё сразу
3. **Тестировать на каждом этапе** - писать тесты параллельно с разработкой
4. **Документировать код** - особенно API и сложную логику
5. **Обращать внимание на безопасность** - валидация, санитизация, логирование
6. **Оптимизировать производительность** - индексы, кэширование, оптимизация запросов

---

## 📝 Дополнительные замечания

### Несоответствия в ТЗ
1. В ТЗ упоминается таблица `roles` и `user_roles` (M:M), но в SQL схеме её нет - это нормально, текущая реализация проще
2. В ТЗ упоминается `search_index`, но в SQL схеме её нет - можно реализовать через PostgreSQL Full-Text Search
3. В ТЗ не описана детально система уведомлений - рекомендуется добавить модель `Notification`

### Улучшения по сравнению с ТЗ
1. Добавить систему уведомлений (не описана в ТЗ, но логично нужна)
2. Использовать современные библиотеки для OAuth (django-allauth)
3. Добавить rate limiting для защиты от злоупотреблений
4. Реализовать кэширование для улучшения производительности

---

*Документ создан на основе анализа текущего состояния проекта и технического задания*

